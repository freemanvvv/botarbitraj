# Construction AI Copilot — План выполнения

## Общая архитектура

```
┌────────────────────────────────────────────────┐
│                  LM Studio                      │
│  ┌─────────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ Qwen3-8B    │ │Llama-Uz  │ │ Qwen3-14B   │ │
│  │ (быстрые    │ │(узбекский│ │ (сметы,     │ │
│  │  ответы)    │ │ диалог)  │ │  код, агент)│ │
│  └──────┬──────┘ └─────┬────┘ └──────┬──────┘ │
│         │              │              │         │
│  ┌──────┴──────────────┴──────────────┴──────┐ │
│  │        OpenAI-compatible API              │ │
│  │        http://localhost:1234/v1           │ │
│  └──────────────────┬────────────────────────┘ │
└─────────────────────┼──────────────────────────┘
                      │
┌─────────────────────┴──────────────────────────┐
│              Python Router (маршрутизатор)       │
│  детект языка → классификация → выбор модели   │
└──────┬──────────────┬──────────────┬────────────┘
       │              │              │
┌──────┴──────┐ ┌─────┴──────┐ ┌────┴──────────┐
│  Phase 1    │ │  Phase 2   │ │  Phase 3      │
│  RAG + Docs │ │  Сметы/BOQ │ │  BIM/IFC      │
│  ChromaDB   │ │  SQLite    │ │  IfcOpenShell │
│  LlamaIndex │ │  openpyxl  │ │  Bonsai/Blend │
└─────────────┘ └────────────┘ └───────────────┘
```

## Сводка этапов

```
Phase 0: Инфраструктура (LM Studio, модели, Python-окружение)
    ↓
Phase 1: RAG-ассистент по нормативам + генерация документов ← СЕЙЧАС
    ↓
Phase 2: Сметы и BOQ с расчётом стоимости
    ↓
Phase 3: BIM / IFC — генерация скриптов-черновиков
    ↓
Post: Масштабирование, оптимизация, документация
```

---

## P0 — Инфраструктура / Bootstrap
**Статус:** [ ] не начато  
**Модели:** —  
**Ожидаемое время:** ~1–2 часа

### Шаги
- [ ] **P0.1** Проверить LM Studio: установлена ли, какая версия, включён ли Apple Metal
- [ ] **P0.2** Скачать модели:
  - `behbudiy/Llama-3.1-8B-Instruct-Uz` (Q4_K_M)
  - `Qwen3-8B` (Q4_K_M)
  - `Qwen3-14B` (Q4_K_M)
  - Embedding: `bge-m3`
- [ ] **P0.3** Настроить GPU Offload в LM Studio (макс, с запасом)
- [ ] **P0.4** Проверить OpenAI API: `GET /models`, `POST /chat/completions`, `POST /embeddings`
- [ ] **P0.5** Создать Python-окружение (venv) + requirements.txt
- [ ] **P0.6** Создать базовый роутер (детект языка + классификация задач)
- [ ] **P0.7** Написать и проверить тестовый запрос к каждой модели через LM Studio API
- [ ] **P0.8** Замерить базовую производительность (память, скорость токенов/с)

**Checkpoint:** LM Studio отвечает, роутер выбирает модель, мы готовы к RAG.

---

## P1 — RAG-ассистент по нормативам + генерация документов
**Статус:** [ ] не начато  
**Модели:** Qwen3-8B, Llama-3.1-8B-Uz, bge-m3  
**Ожидаемое время:** ~4–6 часов

### Компоненты
```
┌─────────────────────────────────────────────┐
│             RAG Pipeline                     │
│                                              │
│  ┌──────────┐    ┌───────────┐              │
│  │  PDF/    │───▶│ LlamaIndex│──▶ ChromaDB   │
│  │ txt docs │    │ (chunks)  │   (vectors)   │
│  └──────────┘    └─────┬─────┘              │
│                        │                     │
│  ┌─────────────────────┴─────────────────┐   │
│  │   Retrieved chunks + user query       │   │
│  │                  │                    │   │
│  │   ┌──────────────┴──────────────┐    │   │
│  │   │    Prompt Assembly          │    │   │
│  │   │  (контекст + инструкция)    │    │   │
│  │   └──────────────┬──────────────┘    │   │
│  └──────────────────┼───────────────────┘   │
│                     │                        │
│          ┌──────────┴──────────┐            │
│          │  LLM (Qwen3-8B /    │            │
│          │  Llama-Uz)          │            │
│          └──────────┬──────────┘            │
│                     │                        │
│          ┌──────────┴──────────┐            │
│          │  Ответ + цитаты     │            │
│          └─────────────────────┘            │
│                                              │
│  ┌─────────────────────────────────────┐    │
│  │     Document Generator              │    │
│  │  Markdown → DOCX (python-docx)     │    │
│  │  Markdown → PDF (weasyprint)       │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

### Шаги
- [ ] **P1.1** Установить зависимости: `chromadb`, `llama-index`, `llama-index-embeddings-lmstudio`, `pdfplumber`, `python-docx`, `weasyprint`
- [ ] **P1.2** Создать модуль загрузки нормативов (PDF → текст → чанки)
- [ ] **P1.3** Создать модуль индексации (embeddings → ChromaDB)
- [ ] **P1.4** Создать модуль поиска + сборки промпта (retrieval + citation)
- [ ] **P1.5** Создать модуль генерации документов (ТЗ, пояснительная записка → DOCX/PDF)
- [ ] **P1.6** Создать CLI-интерфейс (и/или простой web-интерфейс) для диалога
- [ ] **P1.7** Наполнить базу тестовыми нормативами (хотя бы 1–2 документа КМК/СНиП)
- [ ] **P1.8** Протестировать демо-сценарии №2 и №5
- [ ] **P1.9** Протестировать узбекский язык через Llama-Uz

**Checkpoint:** RAG работает, отвечает с цитатами, генерирует PDF/DOCX. Память в норме.

---

## P2 — Сметы и BOQ
**Статус:** [ ] не начато  
**Модели:** Qwen3-14B  
**Ожидаемое время:** ~3–4 часа

### Компоненты
```
┌────────────┐     ┌──────────────────┐     ┌─────────────┐
│  User      │────▶│  LLM (Qwen3-14B) │────▶│  Структура  │
│  описание  │     │  парсит задачу,  │     │  работ и    │
│  объекта   │     │  выдаёт состав   │     │  материалов │
└────────────┘     │  работ          │     └──────┬──────┘
                   └──────────────────┘            │
                                                   │
                   ┌───────────────────────────────┘
                   │
┌──────────────────┴──────────────────┐
│         Python Calculator            │
│  ┌──────────────┐  ┌──────────────┐  │
│  │  SQLite/CSV  │  │  Расчёт      │  │
│  │  расценок    │──▶│  стоимости  │  │
│  └──────────────┘  │  (код, не    │  │
│                     │  LLM!)       │  │
│                     └──────┬───────┘  │
└────────────────────────────┼──────────┘
                             │
              ┌──────────────┴──────────────┐
              │  Export: XLSX (openpyxl)    │
              │  Export: PDF (отчёт)        │
              └─────────────────────────────┘
```

### Шаги
- [ ] **P2.1** Создать базу расценок (SQLite или CSV): материалы, работы, цены
- [ ] **P2.2** Написать парсер структуры BOQ из ответа LLM
- [ ] **P2.3** Написать модуль расчёта стоимости (арифметика в коде)
- [ ] **P2.4** Написать модуль экспорта XLSX (openpyxl) + PDF
- [ ] **P2.5** Написать CLI-интерфейс для смет
- [ ] **P2.6** Протестировать демо-сценарий №3

**Checkpoint:** Смета считается кодом, не LLM. XLSX и PDF выгружаются.

---

## P3 — BIM / IFC (экспериментально)
**Статус:** [ ] не начато  
**Модели:** Qwen3-14B  
**Ожидаемое время:** ~4–5 часов

### Компоненты
```
┌────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  User      │────▶│  LLM (Qwen3-14B) │────▶│  Python-скрипт   │
│  параметры │     │  генерирует      │     │  под IfcOpenShell│
│  здания    │     │  код IFC/SVG     │     │  или SVG/DXF     │
└────────────┘     └──────────────────┘     └────────┬─────────┘
                                                      │
                              ┌───────────────────────┤
                              │                       │
                   ┌──────────┴──────────┐  ┌────────┴────────┐
                   │  IfcOpenShell       │  │  ezdxf / svg    │
                   │  → IFC-файл         │  │  → DXF / SVG    │
                   │  → Bonsai (Blender) │  │                 │
                   └─────────────────────┘  └─────────────────┘
```

### Шаги
- [ ] **P3.1** Установить IfcOpenShell + ezdxf
- [ ] **P3.2** Написать исполнитель Python-скриптов (безопасный sandbox)
- [ ] **P3.3** Создать модуль генерации SVG-планов (сетка осей, контур, помещения)
- [ ] **P3.4** Создать модуль генерации IFC-заготовок (стены, перекрытия, проёмы)
- [ ] **P3.5** Подготовить Bonsai для визуальной проверки
- [ ] **P3.6** Протестировать демо-сценарий №4 (SVG-план)
- [ ] **P3.7** Протестировать демо-сценарий №1 (скрипт-заготовка IFC)

**Checkpoint:** SVG-план рисуется, IFC-файл создаётся и открывается в Bonsai. Оба — черновики.

---

## Post — Документация, замеры, план переноса
- [ ] Архитектура системы + диаграммы взаимодействия
- [ ] Инструкция установки LM Studio и сервисов
- [ ] Настройки моделей и агентов
- [ ] Python-примеры (подключение, стриминг, роутинг, инструменты)
- [ ] Замеры производительности (память, скорость, Metal)
- [ ] План переноса на сервер (RTX 4090 / Mac Studio)
- [ ] Список ограничений

---

## Запуск воркфлоу

Я (Tron) буду выполнять каждую фазу последовательно. После каждого этапа — докладываю результат. Ты можешь в любой момент:

- `!статус` — текущий прогресс
- `!пропусти [фаза]` — форсировать переход
- `!правки [текст]` — скорректировать подход
